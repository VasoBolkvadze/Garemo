extends ../../index
block content
	div.jumbotron.jumbotron-ultra-sm
		div.container
			div.row
				div.col-sm-12.col-lg-12
					h2.txt-capital წარმოდგენილი ათვისების გეგმები
	div.container
		div.row
			div.panel.panel-default.fit
				div.panel-body
					table.table.table-hover.table-bordered.table-striped
						thead.txt-capital
							th(width='5%') ID
							th(width='25%') ლიცენზიის ID
							th(width='25%') ჩანაწერების რაოდენობა
							th(width='25%') წარმოდგენის თარიღი
							th(width='20%') სტატუსი
						each item in items
							tr.grid-item
								td
									a(href=item.edit)= item.id
								td= item.licenziisId
								td= item.chanacerebisRaodenoba
								td= item.carmodgenisTarigi
								td= item.statusi